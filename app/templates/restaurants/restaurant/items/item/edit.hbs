<div class="container">
  <div class="row">
    <div class="col-lg-8 col-lg-offset-2">
      <div class="page-header">
        <h1>Edit {{model.name}}</h1>
      </div>
      <div class="new-form">
        <form class="form-horizontal well">
          <fieldset>
            <div class="form-group">
              <label for="inputName" class="col-xs-2 control-label">Name</label>
              <div class="col-xs-10">
                {{input type="text" placeholder="Name" value=model.name class="form-control"}}
              </div>
            </div>
            <div class="form-group">
              <label for="inputEmail" class="col-xs-2 control-label">Description</label>
              <div class="col-xs-10">
                {{input type="text" placeholder="Description" value=model.description class="form-control"}}
              </div>
            </div>   
            <div class="form-group">
              <label for="inputEmail" class="col-xs-2 control-label">Price</label>
              <div class="col-xs-10">
                {{input type="text" placeholder="Price" value=model.price class="form-control"}}
              </div>
            </div>          
            <div class="form-group">
              <label for="inputProfilePicture" class="col-xs-2 control-label">Item Picture URL</label>
              <div class="col-xs-10">
                {{input type="url" placeholder="URL to image" value=model.item_picture class="form-control"}}
              </div>
            </div>
            <div class="form-group text-center">
              <div class="col-sm-4 col-sm-offset-4">
                {{#link-to 'restaurants.restaurant' class="btn btn-default" activeClass=""}}Cancel{{/link-to}}
                <button {{action 'editItem'}} class="btn btn-primary">Submit</button>
              </div>
            </div>
          </fieldset>
        </form>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-lg-6 col-lg-offset-3">
      <div class="panel panel-danger text-center">
        <div class="panel-heading">
          <h3 class="panel-title">The danger zone {{fa-icon "warning"}}</h3>
        </div>
        <div class="panel-body">
          Completely and <strong>irreversably</strong> remove item from the data store.
          <button {{action 'deleteItem'}} class="btn btn-danger">Delete</button>
        </div>
      </div>
    </div>
  </div>
</div>