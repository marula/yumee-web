<div class="container">
  <div class="new-form">
    <form class="form-horizontal well">
      <fieldset>
        <legend>Editing restaurant</legend>
        <div class="form-group">
          <div class="col-xs-12 col-sm-4">
            {{input type="text" placeholder="Name" value=model.name class="name-input"}}
          </div>
          <div class="col-xs-12 col-sm-4">
            {{input type="text" placeholder="Email" value=model.email class="name-input"}}
          </div>
          <div class="col-xs-12 col-sm-4">
            {{#link-to 'restaurants' class="btn btn-default" activeClass=""}}Cancel{{/link-to}}
            <button {{action 'editRestaurant'}} class="btn btn-primary">Submit</button>
          </div>
        </div>
      </fieldset>
    </form>
  </div>
</div>